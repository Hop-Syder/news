# FastAPI & Web Framework
fastapi==0.110.1
uvicorn==0.25.0
python-multipart>=0.0.9

# Supabase 
supabase==2.15.0

# Pydantic & Settings
pydantic>=2.6.4
pydantic-settings>=2.2.1
email-validator>=2.2.0

# Environment & Config
python-dotenv>=1.0.1

# Security & Auth (JWT handled by Supabase, but we keep these for compatibility)
python-jose>=3.3.0
passlib>=1.7.4
bcrypt==4.1.3

# HTTP & Requests
requests>=2.31.0
requests-oauthlib>=2.0.0

# Utilities
typer>=0.9.0
tzdata>=2024.2

# Cloud & Storage (AWS S3 if needed alongside Supabase Storage)
boto3>=1.34.129

# Data Processing (if needed)
pandas>=2.2.0
numpy>=1.26.0

# Development & Testing
pytest>=8.0.0
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0

# Cryptography
cryptography>=42.0.8
pyjwt>=2.10.1

# JSON Query (if needed)
jq>=1.6.0
